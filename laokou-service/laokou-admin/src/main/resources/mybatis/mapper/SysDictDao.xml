<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.laokou.admin.domain.sys.repository.dao.SysDictDao">
    <select id="getDictList" resultType="io.laokou.admin.interfaces.vo.DictVO">
        SELECT
id
,create_date
,dict_value
,dict_label
,type
,`status`
,remark
from boot_sys_dict
where del_flag = '0' and `status` = '0'
<if test="qo.type != null and qo.type != ''">
    and type = #{qo.type}
</if>
order by create_date desc
    </select>
</mapper>