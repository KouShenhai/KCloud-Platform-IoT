syntax = "proto3";

option java_multiple_files = true;
option java_package = "org.laokou.distributed.id.proto";
option java_outer_classname = "DistributedIdProto";

message DistributedIdCmd {
	int32 num = 1;
}

message DistributedIdResult {
	string code = 1;
	string msg = 2;
	int64 data = 3;
}

 message DistributedIdsResult {
	string code = 1;
	string msg = 2;
	repeated int64 data = 3;
}

service DistributedIdServiceI {
	rpc generateId(DistributedIdCmd) returns (DistributedIdResult);
	rpc generateIds(DistributedIdCmd) returns (DistributedIdsResult);
}
