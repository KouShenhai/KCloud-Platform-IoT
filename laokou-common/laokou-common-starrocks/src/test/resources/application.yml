spring:
  application:
    name: laokou-common-starrocks
  profiles:
    active: test
  datasource:
    dynamic:
      datasource:
        starrocks:
          type: com.zaxxer.hikari.HikariDataSource
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://starrocks:9030/test?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
          username: root
          password: laokou123

mybatis-plus:
  # 全局处理
  global-config:
    db-config:
      column-format: %s
  tenant:
    enabled: true
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
