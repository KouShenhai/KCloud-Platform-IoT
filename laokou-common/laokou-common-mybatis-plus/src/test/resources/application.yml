spring:
  application:
    name: laokou-common-mybatis-plus
  profiles:
    active: test
  threads:
    virtual:
      enabled: true
  datasource:
    dynamic:
      datasource:
        master:
          type: com.zaxxer.hikari.HikariDataSource
          url: jdbc:tc:postgresql:latest:///kcloud_platform_test
          username: root
          password: laokou123

mybatis-plus:
  # 全局处理
  global-config:
    db-config:
      column-format: "\"%s\""
  tenant:
    enabled: true
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
