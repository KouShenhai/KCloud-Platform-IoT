version: 2.1

jobs:
  build:
    docker:
      - image: azul/zulu-openjdk:25-jre-latest

    environment:
      JVM_OPTS: -Xmx2048m
      TERM: dumb

    steps:
      - checkout
      - run: mvn clean install -P dev -DskipTests -B
workflows:
  maven-build:
    jobs:
      - build
