<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.laokou.sys.mapper.SysDictMapper">


    <select id="queryPageList" resultType="org.laokou.sys.vo.SysDictVO">
        SELECT
        id,
        dict_value, dict_label, type, remark, sort
        from boot_sys_dict
        where del_flag = 0
            <if test="qo.dictValue != null and qo.dictValue != ''">
                    and `dict_value` = #{qo.dictValue}
            </if>
            <if test="qo.dictLabel != null and qo.dictLabel != ''">
                    and `dict_label` like concat('%',#{qo.dictLabel},'%')
            </if>
            <if test="qo.type != null and qo.type != ''">
                    and `type` = #{qo.type}
            </if>
            <if test="qo.remark != null and qo.remark != ''">
                    and `remark` like concat('%',#{qo.remark},'%')
            </if>
            <if test="qo.sort != null and qo.sort != ''">
                    and `sort` = #{qo.sort}
            </if>
            order by sort asc
    </select>

    <select id="resultList" fetchSize="1000" resultSetType="FORWARD_ONLY" resultType="org.laokou.sys.vo.SysDictVO">
        SELECT

        dict_value, dict_label, type, remark, sort
        from boot_sys_dict
        where del_flag = 0
            <if test="qo.dictValue != null and qo.dictValue != ''">
                    and `dict_value` = #{qo.dictValue}
            </if>
            <if test="qo.dictLabel != null and qo.dictLabel != ''">
                    and `dict_label` like concat('%',#{qo.dictLabel},'%')
            </if>
            <if test="qo.type != null and qo.type != ''">
                    and `type` = #{qo.type}
            </if>
            <if test="qo.remark != null and qo.remark != ''">
                    and `remark` like concat('%',#{qo.remark},'%')
            </if>
            <if test="qo.sort != null and qo.sort != ''">
                    and `sort` = #{qo.sort}
            </if>
    </select>
</mapper>
