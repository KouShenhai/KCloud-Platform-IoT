node {
  name = "emqx@127.0.0.1"
  cookie = "emqxsecretcookie"
  data_dir = "data"
}

cluster {
  name = emqxcl
  discovery_strategy = manual
}

# 设置控制台端口和默认账号
dashboard {
    listeners.http {
        bind = 18083
    }
}
